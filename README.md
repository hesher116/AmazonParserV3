# Multi-Agent Amazon Parser V3

Професійний багатоагентний парсер для збору даних з продуктів Amazon з веб-інтерфейсом на Flask.

## 🚀 Основні можливості

### Агенти парсингу

1. **Image Parser Agent** - Парсинг зображень:
   - Hero зображення (головне зображення продукту)
   - Product Gallery (галерея зображень продукту в високій якості)
   - A+ Content - From the Brand (зображення з секції "From the brand")
   - A+ Content - Product Description (зображення з опису продукту)
   - Автоматична дедуплікація зображень (MD5 hash)
   - Завантаження в максимальній роздільній здатності

2. **Text Parser Agent** - Парсинг текстової інформації:
   - Назва продукту
   - Бренд
   - Ціна (поточна та стара)
   - ASIN
   - Product Overview (таблиця)
   - About this item (bullets)
   - Ingredients
   - Important Information
   - Technical Details
   - Product Details

3. **Customer Reviews Agent** - Парсинг відгуків:
   - Загальна статистика (рейтинг, кількість відгуків, розподіл по зірках)
   - Детальні відгуки (ім'я, рейтинг, текст, дата, verified purchase)
   - Зображення з відгуків

4. **Q&A Parser Agent** - Парсинг питань та відповідей

5. **Variant Detector Agent** - Детекція варіантів:
   - Автоматичне виявлення варіантів (Color/Size/Style)
   - Парсинг кожного варіанту окремо

6. **Validator Agent** - Валідація зібраних даних

### Веб-інтерфейс

- Простий та зручний Flask UI
- Вибір категорій парсингу через чекбокси
- Прогрес-бар для відстеження процесу
- Історія задач з можливістю перегляду результатів
- Автоматичне оновлення статусу через AJAX

### Вихідні дані

- **Структурована файлова система:**
  ```
  outputs/
    {product_name}/
      hero/
        hero.jpg
      product/
        product1.jpg
        product2.jpg
        ...
      aplus_brand/
        brand1.jpg
        ...
      aplus_product/
        A+1.jpg
        ...
      QAImages/
        review1.jpg
        ...
      {product_name}.docx
  ```

- **DOCX документ** з усією зібраною інформацією:
  - Назва продукту та URL
  - Вся текстова інформація
  - Відгуки та Q&A
  - Зображення в правильному порядку (hero → product → aplus_brand → aplus_product → QAImages)

## 🛠 Технології

- **Python 3.8+**
- **Flask 3.0.0** - веб-фреймворк
- **Selenium 4.15.2** - автоматизація браузера
- **undetected-chromedriver 3.5.4** - обхід детекції автоматизації
- **python-docx 1.1.0** - генерація Word документів
- **SQLite** - база даних для історії задач
- **Pillow 10.1.0** - обробка зображень

## 📦 Встановлення

1. **Клонуйте репозиторій:**
   ```bash
   git clone https://github.com/hesher116/AmazonParserV3.git
   cd AmazonParserV3
   ```

2. **Створіть віртуальне середовище:**
   ```bash
   python -m venv venv
   ```

3. **Активуйте віртуальне середовище:**
   - Windows:
     ```bash
     venv\Scripts\activate
     ```
   - Linux/Mac:
     ```bash
     source venv/bin/activate
     ```

4. **Встановіть залежності:**
   ```bash
   pip install -r requirements.txt
   ```

5. **Налаштуйте змінні оточення (опціонально):**
   ```bash
   cp .env.example .env
   # Відредагуйте .env файл за потреби
   ```

## 🚀 Запуск

```bash
python run.py
```

Відкрийте браузер та перейдіть на `http://127.0.0.1:5000`

## ⚙️ Конфігурація

Налаштування знаходяться в `config/settings.py` та можуть бути змінені через змінні оточення:

- `AMAZON_PARSER_RATE_LIMIT_MIN` - мінімальна затримка між діями (за замовчуванням: 1.5 сек)
- `AMAZON_PARSER_RATE_LIMIT_MAX` - максимальна затримка між діями (за замовчуванням: 4.0 сек)
- `AMAZON_PARSER_MAX_RETRIES` - кількість повторних спроб (за замовчуванням: 3)
- `AMAZON_PARSER_HEADLESS` - режим безголового браузера (за замовчуванням: false)
- `AMAZON_PARSER_TIMEOUT` - таймаут очікування елементів (за замовчуванням: 15 сек)
- `AMAZON_PARSER_OUTPUT_DIR` - директорія для збереження результатів (за замовчуванням: outputs)
- `AMAZON_PARSER_DATABASE_PATH` - шлях до бази даних (за замовчуванням: tasks.db)

## 🎯 Особливості реалізації

### Anti-Detection

- Випадкові User-Agents з пулу
- Випадкові затримки між діями
- Обробка soft blocks (кнопка "Continue shopping")
- Headless режим (опціонально)

### Оптимізація швидкості

- WebDriverWait для очікування завантаження зображень замість фіксованих затримок
- Раннє вихід з A+ scanning якщо секції не знайдені
- Оптимізований пошук секцій (спочатку специфічні селектори)
- Логіка каруселі з детекцією дублікатів

### Обробка помилок

- Retry логіка для кожного агента (3 спроби)
- Graceful degradation - часткові результати зберігаються
- Структуроване логування
- Автоматичне закриття модальних вікон

### Якість даних

- MD5 дедуплікація зображень
- Перевірка мінімального розміру зображень
- Фільтрація рекламних фраз
- Валідація зібраних даних

## 📁 Структура проекту

```
AmazonParserV3/
├── agents/              # Агенти парсингу
│   ├── image_parser.py
│   ├── text_parser.py
│   ├── reviews_parser.py
│   ├── qa_parser.py
│   ├── variant_detector.py
│   └── validator.py
├── core/                # Основна логіка
│   ├── coordinator.py   # Координатор агентів
│   ├── browser_pool.py  # Управління браузером
│   ├── database.py      # SQLite операції
│   └── docx_generator.py # Генерація DOCX
├── web/                 # Веб-інтерфейс
│   ├── app.py           # Flask додаток
│   ├── templates/
│   │   └── index.html
│   └── static/
│       └── style.css
├── utils/               # Утиліти
│   ├── file_utils.py    # Робота з файлами
│   ├── text_utils.py    # Обробка тексту
│   └── logger.py        # Логування
├── config/              # Конфігурація
│   └── settings.py
├── outputs/             # Результати парсингу
├── requirements.txt
├── .env.example
├── .gitignore
├── run.py               # Точка входу
└── README.md
```

## 🔧 Використання

1. Відкрийте веб-інтерфейс на `http://127.0.0.1:5000`
2. Введіть URL продукту Amazon
3. Виберіть категорії для парсингу (чекбокси)
4. Натисніть "Start Parsing"
5. Відстежуйте прогрес через прогрес-бар
6. Результати будуть збережені в `outputs/{product_name}/`

## 📝 Примітки

- Програма автоматично обробляє кнопку "Continue shopping" якщо вона з'являється
- Якщо продукт має варіанти, кожен варіант буде спарсений окремо
- Папки створюються тільки якщо знайдені відповідні дані
- Якщо папка з назвою продукту вже існує, буде створена з суфіксом (2), (3) тощо
- DOCX файл завжди генерується з назвою продукту (не ASIN)

## 🐛 Відомі обмеження

- Парсинг відгуків обмежений тими, що відображаються на сторінці (майбутнє: опція для завантаження більшої кількості)
- Деякі A+ секції можуть бути пропущені якщо вони мають нестандартну структуру
- CAPTCHA потребує ручного втручання

## 📄 Ліцензія

Цей проект розроблений для особистого використання.

## 👤 Автор

hesher116

## 🔄 Оновлення

Останнє оновлення: 2025-01-XX

### Останні зміни:
- Оптимізовано швидкість парсингу thumbnail images
- Додано автоматичну обробку кнопки "Continue shopping"
- Виправлено генерацію назв папок та DOCX файлів
- Прискорено A+ content scanning
- Додано URL в DOCX документ
- Виправлено порядок зображень в DOCX (по черзі від першого до останнього)

